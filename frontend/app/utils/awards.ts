// The one place the Duty Master badge icon is defined — everywhere it's shown (Home,
// member award-history page) reads it from here rather than repeating the literal emoji.
export const DUTY_MASTER_BADGE_EMOJI = '🏆'
